DATABASE_URL=postgresql+psycopg2://boba:boba@localhost:5433/boba

# Optional: a free Socrata app token lifts DOHMH pull rate limits.
# SOCRATA_APP_TOKEN=

# Yelp Fusion key -> the primary shop-discovery source, plus is_closed for free
# (free tier ~500 calls/day; get one at yelp.com/developers).
# Without it, `just ingest-yelp` no-ops and the set is just the DOHMH name-match tail.
# YELP_API_KEY=
