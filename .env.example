DATABASE_URL=postgresql+psycopg2://boba:boba@localhost:5433/boba

# Optional: a free Socrata app token lifts DOHMH pull rate limits.
# SOCRATA_APP_TOKEN=

# Optional: Yelp Fusion key -> current open/closed status (yelp.com/developers).
# Without it, `just ingest-yelp` is a no-op.
# YELP_API_KEY=
